import { Config } from "../../core/config";
import { ServiceMap } from "../../main.d";

export async function initRESTDelivery(
   config: Config,
   serviceMap: ServiceMap
) {
    // TODO: Implement REST delivery using Express or Fastify
}