{
  "name": "ts-template",
  "version": "1.0.0",
  "description": "Generic Template for Node TS Project",
  "main": "src/main.ts",
  "scripts": {
    "dev": "nodemon src/main.ts",
    "build-ts": "tsc",
    "build": "npm run build-ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "xritzx",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.5",
    "nodemon": "^3.0.1"
  }
}
